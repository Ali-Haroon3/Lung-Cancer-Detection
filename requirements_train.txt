# Dependencies for the OFFLINE trainer (train.py) only.
# The deployed app uses requirements.txt and does NOT need these.
# TensorFlow 2.15 requires Python <= 3.11, so run train.py in such an env.
-r requirements.txt
datasets==2.16.1
